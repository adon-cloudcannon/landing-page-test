<a href="{{ url }}" class="transition border border-white rounded-xl bg-blackish p-6 flex  hover:bg-blue-900 group mt-4 first:mt-0">
	<span class="inline-block flex-1">{{ title }}</span>
	<span class="w-5 group-hover:translate-x-2 flex justify-center transition">{{ '/images/icons/outline/arrow-right.svg' | svgContents }}</span>
</a>