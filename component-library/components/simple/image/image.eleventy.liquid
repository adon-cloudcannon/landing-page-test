{% assign classes = "" %}

{% if scale %}
	{% assign classes = classes | append: " scale-[" | append: scale | append: "]" %}
{% endif %}

{% if margin.top %}
	{% assign classes = classes | append: " mt-[" | append: margin.top | append: "px]" %}
{% endif %}

{% if margin.left %}
	{% assign classes = classes | append: " ml-[" | append: margin.left | append: "px]" %}
{% endif %}

{% if margin.bottom %}
	{% assign classes = classes | append: " mb-[" | append: margin.bottom | append: "px]" %}
{% endif %}

{% if margin.right %}
	{% assign classes = classes | append: " mr-[" | append: margin.right | append: "px]" %}
{% endif %}

{% if ontop %}
	{% assign classes = classes | append: " relative z-10" %}
{% endif %}

{% if center %}
	{% assign classes = classes | append: " mx-auto block" %}
{% endif %}

{% if rounded %}
	{% assign classes = classes | append: " rounded-2xl" %}
{% endif %}

{% if border %}
	{% assign classes = classes | append: " border" %}
{% endif %}

<img src="{{ image }}" alt="{{ alt }}" class="{{ classes }}">